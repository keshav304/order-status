datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model orderlist {
  id         Int      @id @default(autoincrement())
  customer   String
  status     String
}